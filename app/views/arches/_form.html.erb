<%= form_for(@arch) do |f| %>
  <% if @arch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arch.errors.count, "error") %> prohibited this arch from being saved:</h2>

      <ul>
      <% @arch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
