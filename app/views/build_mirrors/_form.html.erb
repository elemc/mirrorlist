<%= form_for(@build_mirror) do |f| %>
  <% if @build_mirror.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@build_mirror.errors.count, "error") %> prohibited this build_mirror from being saved:</h2>

      <ul>
      <% @build_mirror.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
